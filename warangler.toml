# wrangler.toml - 解决compatibility_date必填+名称匹配问题
main = "./index.js"                # 你的Worker入口文件（根目录的index.js）
name = "backblaze-b2-read"         # 和CF的Worker应用名一致，解决名称警告
compatibility_date = "2025-12-23"  # 兼容日期，填当天日期（格式YYYY-MM-DD）